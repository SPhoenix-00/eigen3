# Eigen3 dependencies
# -------------------
# Choose one file and install from the repo root (where this file lives):
#
#   pip install -r requirements-data.txt
#       NumPy + Pandas only. Use for process_eigen_data.py on any OS,
#       including native Windows.
#
#   pip install -r requirements-train.txt
#       EvoRL (editable) + Eigen3 + training/visualization extras.
#       Requires Linux, macOS, or WSL (orbax / uvloop are not Windows-friendly).
#
#   pip install -r requirements-dev.txt
#       Same as train, plus pytest, black, flake8, mypy, etc.
#
# Default below matches the former single-file workflow: full dev install.

-r requirements-dev.txt
